<view class="article-detail">
  <view class="title">更改存储时间格式</view>
  <view>
    <p>datetime 类型在mysql中默认精度只到秒解决方法如下</p>
    <p>在 4.2的Release Notes有下面一句话：</p>
    <p>Added support for fractional seconds for MySQL 5.6 and above</p>
    <p>可以看出 4.2已经支持日期精度功能，关于mysql的fractional seconds功能可以参考 mysql reference.</p>
    <p>我们在Rails可以通过添加migrate文件来解决，具体如下：</p>
  </view>
  <view class="pre-container">
    <rich-text nodes="{{changeDatePreData[0]}}">
    </rich-text>
  </view>
  <view>在生成的文件中修改如下</view>
  <view class="pre-container">
    <rich-text nodes="{{changeDatePreData[1]}}">
    </rich-text>
  </view>
  <view class="pre-container">
    <rich-text nodes="{{changeDatePreData[2]}}">
    </rich-text>
  </view>
  <view class="pre-container">
    <rich-text nodes="{{changeDatePreData[2]}}">
    </rich-text>
  </view>
</view>